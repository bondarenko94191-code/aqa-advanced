const array = [1, 2, 3];
const sum = array.reduce((accumulator, number) => accumulator + number, 0);
const average = sum / 2;
function calculation(array) {
    if (array.length === 0) {
        return 0;
    }
}
console.log(average);
